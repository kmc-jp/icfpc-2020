#include <iostream>
#include <string>
#include <algorithm>
#include <iostream>

using namespace std;

string string_of_int(int x);

string parse_list(const string &s, int &idx);

string modulate(const string s);